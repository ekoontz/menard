[{:rule :dont-inflect-pronouns
  :if {:cat :noun
       :pronoun true}
  :then [{:inflected? true
          :subcat []}]}
 {:rule :dont-inflect-propernouns
  :if {:cat :noun
       :propernoun true}
  :then [{:inflected? true
          :subcat []}]}

 {:rule :subject-verb-agreement
  :if {:cat :verb}
  :then
  (let [agr (atom :top)
        mod (atom :top)
        pred (atom :top)
        ref (atom :top)]
    [{:sem {:subj {:pred pred
                   :mod mod
                   :ref ref}}
      :agr agr
      :subcat {:1 {:cat :noun
                   :case :nom
                   :sem {:pred pred
                         :mod mod
                         :ref ref}
                   :agr agr}}}])}]

